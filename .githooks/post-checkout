#!/usr/bin/env bash
set -euo pipefail

if git diff --name-only "$1" "$2" 2>/dev/null | grep -Eq '(^package-lock\.json$|^package\.json$)'; then
  echo "Dependencies changed. Run npm install before continuing." >&2
fi
